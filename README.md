# Как запустить приложение

<!-- TOC -->
* [Как запустить приложение](#как-запустить-приложение)
  * [1. Запуск](#1-запустите-приложение)
      * [Предварительные требования](#предварительные-требования)
      * [Установка Docker на Windows (WSL 2)](#установка-docker-на-windows-wsl-2)
    * [Запуск приложения](#запуск-приложения)
      * [1. Клонирование репозитория](#1-клонируйте-репозиторий)
      * [2. Запуск всех сервисов](#2-запустите-все-сервисы)
      * [3. Открытие приложения](#3-откройте-приложение)
      * [4. Остановка приложения](#4-остановка-приложения)
  * [2. Запуск тестов из корневой директории](#2-запуск-тестов-из-корневой-директории)
* [Реализованные сценарии тестирования](#реализованные-сценарии-тестирования)
  * [Регистрация пользователя](#регистрация-пользователя)
  * [Авторизация пользователя](#авторизация-пользователя)
  * [Профиль пользователя](#профиль-пользователя)
  * [Перевод средств](#перевод-средств)
  * [История транзакций и пополнение баланса](#история-транзакций-и-пополнение-баланса)
<!-- TOC -->


## 1. Запустите приложение:
#### Предварительные требования

Перед началом работы убедитесь, что на вашем компьютере установлены следующие инструменты:

| Инструмент                                                        | Версия           | Как проверить            |
|-------------------------------------------------------------------|------------------|--------------------------|
| [Docker Desktop](https://www.docker.com/products/docker-desktop/) | любая актуальная | `docker --version`       |
| [Docker Compose](https://docs.docker.com/compose/)                | v2+              | `docker compose version` |
| [Node.js](https://nodejs.org/)                                    | 18+              | `node --version`         |
| [Git](https://git-scm.com/)                                       | любая            | `git --version`          |

> **Пользователям Windows:** используйте PowerShell или Git Bash для всех команд ниже.

---

#### Установка Docker на Windows (WSL 2)

Docker Desktop на Windows работает через **WSL 2** (Windows Subsystem for Linux). Если вы видите ошибку `Docker Desktop - WSL not installed` - выполните шаги ниже.

##### Шаг 1. Включите WSL

Откройте **PowerShell от имени администратора** (правая кнопка мыши → «Запуск от имени администратора») и выполните:

```powershell
wsl --install
```

Эта команда автоматически:
- включит компонент WSL
- установит Ubuntu как дистрибутив по умолчанию

**Перезагрузите компьютер** после завершения установки.

> Если команда `wsl --install` не работает (старая версия Windows), включите WSL вручную:
> ```powershell
> dism.exe /online /enable-feature /featurename:Microsoft-Windows-Subsystem-Linux /all /norestart
> dism.exe /online /enable-feature /featurename:VirtualMachinePlatform /all /norestart
> ```
> После этого перезагрузитесь и установите WSL 2 kernel update:
> [Скачать обновление ядра WSL 2](https://aka.ms/wsl2kernel)

##### Шаг 2. Установите WSL 2 как версию по умолчанию

После перезагрузки снова откройте PowerShell **от имени администратора**:

```powershell
wsl --set-default-version 2
```

Проверьте, что WSL работает:

```powershell
wsl --list --verbose
```

Должны увидеть Ubuntu со статусом `Running` и версией `2`.

##### Шаг 3. Установите Docker Desktop

1. Скачайте [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop/)
2. Запустите установщик и следуйте инструкциям
3. На экране настройки убедитесь, что отмечен пункт **«Use WSL 2 instead of Hyper-V»**
4. После установки **перезагрузите компьютер**

##### Шаг 4. Запустите Docker Desktop

- Откройте Docker Desktop из меню «Пуск»
- Дождитесь, пока в трее появится иконка Docker (кит) и статус сменится на **«Engine running»**
- Только после этого выполняйте команды `docker compose`

##### Шаг 5. Проверьте установку

```powershell
docker --version
docker compose version
```

Обе команды должны вернуть версию без ошибок. Если все работает - переходите к разделу **«Запуск приложения»**.

#### Частые проблемы

| Ошибка                                               | Решение                                                                        |
|------------------------------------------------------|--------------------------------------------------------------------------------|
| `WSL not installed`                                  | Выполните `wsl --install` в PowerShell **от администратора** и перезагрузитесь |
| `Cannot connect to the Docker daemon`                | Docker Desktop не запущен - откройте его из меню «Пуск» и дождитесь старта     |
| `Hardware assisted virtualization... is not enabled` | Включите виртуализацию в BIOS/UEFI (пункт Intel VT-x или AMD-V)                |
| После `wsl --install` Ubuntu не открывается          | Установите Ubuntu вручную из [Microsoft Store](https://aka.ms/wslubuntu)       |

---

### Запуск приложения

#### 1. Клонируйте репозиторий

```bash
git clone <ссылка-на-репозиторий>
cd <папка-репозитория>
```

#### 2. Запустите все сервисы

```bash
docker compose up -d --build
```

Команда собирает и запускает четыре контейнера:
- **database** - PostgreSQL
- **app** - бэкенд на Python/FastAPI (порт 8080 внутри сети)
- **client** - фронтенд на Vue.js (статическая сборка)
- **web-proxy** - Nginx, раздает все на **http://localhost**

Первая сборка может занять 2–5 минут. Последующие запуски - быстрее.

#### 3. Откройте приложение

Перейдите по адресу **http://localhost** в браузере.

Вы должны увидеть страницу входа F2F Bank.

#### 4. Остановка приложения

```bash
docker compose down
```

Полный сброс вместе с данными базы данных:

```bash
docker compose down -v
```

---
## 2. Запуск тестов из корневой директории:
```bash
cd tests/
```

```bash
npm i
```

```bash
npx playwright test
```

Запуск в UI-режиме (визуальный, удобен для отладки):

```bash
npx playwright test --ui
```

Генерация отчета после прогона:

```bash
npx playwright show-report
```
---
# Реализованные сценарии тестирования
## Регистрация пользователя

| Сценарий | Входные данные | Ожидаемый результат | Критичность |
|----------|----------------|---------------------|-------------|
| Регистрация пользователя с валидными данными | Корректные имя, фамилия, email и пароль | Пользователь успешно зарегистрирован и перенаправлен на страницу входа | Критический |
| Регистрация с существующим email | Email уже зарегистрированного пользователя | Регистрация отклонена, отображается сообщение «User with this email already exists» | Критический |
| Регистрация с пустыми полями | Все поля формы пустые | Форма не отправляется, пользователь остается на странице регистрации | Критический |
| Регистрация с некорректным форматом email | Email без символа `@` | Браузерная валидация предотвращает отправку формы | Высокий |
| Регистрация со слишком коротким паролем | Пароль менее 8 символов | Форма не отправляется, отображается ошибка валидации | Высокий |

---

## Авторизация пользователя

| Сценарий | Входные данные | Ожидаемый результат | Критичность |
|----------|----------------|---------------------|-------------|
| Вход с валидными данными | Зарегистрированный email и корректный пароль | Пользователь успешно авторизован и перенаправлен на главную страницу | Критический |
| Вход с неверным паролем | Существующий email и неверный пароль | Возвращается **401 Unauthorized**, пользователь остается на странице входа | Критический |
| Вход с несуществующим email | Незарегистрированный email | Возвращается **401 Unauthorized**, пользователь остается на странице входа | Критический |
| Попытка входа с пустыми полями | Пустые email и пароль | Форма не отправляется, пользователь остается на странице входа | Средний |
| Валидация некорректного формата email | Email некорректного формата | Браузерная валидация предотвращает отправку формы | Высокий |
| Выход из аккаунта | Авторизованный пользователь | Пользователь успешно выходит из системы и перенаправляется на страницу входа | Критический |

---

## Профиль пользователя

| Сценарий | Входные данные | Ожидаемый результат | Критичность |
|----------|----------------|---------------------|-------------|
| Отображение корректных данных в профиле | Авторизованный пользователь | Отображаются корректные имя, фамилия и email пользователя | Высокий |

---

## Перевод средств

| Сценарий | Входные данные | Ожидаемый результат | Критичность |
|----------|----------------|---------------------|-------------|
| Попытка перевода средств при нулевом балансе | Баланс = 0, корректные данные перевода | Перевод не выполняется, отображается сообщение «Transfer failed. Check your balance.» | Критический |
| Попытка перевода средств при ненулевом балансе | Пополненный баланс, корректные данные перевода | Перевод успешно выполнен | Критический |
| Перевод по номеру телефона со скобками и дефисами | Пополненный баланс, номер в формате `+7 (999) 123-45-67` | Перевод успешно выполнен | Средний |
| Ошибка валидации номера телефона без знака «+» | Номер телефона без `+` | Отображается сообщение о неверном формате номера телефона | Высокий |
| Ошибка валидации при переводе суммы, равной нулю | Сумма перевода = 0 | Отображается сообщение «Amount must be greater than zero» | Высокий |
| Ошибка валидации при переводе отрицательной суммы | Сумма перевода < 0 | Отображается сообщение «Amount must be greater than zero» | Высокий |

---

## История транзакций и пополнение баланса

| Сценарий | Входные данные | Ожидаемый результат | Критичность |
|----------|----------------|---------------------|-------------|
| Отображение пустой истории транзакций | Новый пользователь без операций | Отображается сообщение об отсутствии транзакций, баланс равен 0 | Средний |
| Успешное пополнение баланса | Положительная сумма пополнения | Баланс увеличивается, запись появляется в истории транзакций | Критический |
| Пополнение баланса с пустым вводом | Пустое поле суммы | Отображается сообщение «Please enter an amount», баланс не изменяется | Высокий |
| Пополнение баланса отрицательной суммой | Отрицательная сумма | Отображается сообщение «Amount must be positive», баланс не изменяется | Высокий |
| Пополнение баланса нулевой суммой | Сумма = 0 | Отображается сообщение «Amount must be greater than zero», баланс не изменяется | Средний |

---
Всего реализовано **23 автоматизированных E2E-теста**.
